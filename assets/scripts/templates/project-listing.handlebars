{{#each projects as |project|}}
  {{log project}}

  <div>
    <h1 data-id="{{project.id}}"> <small>{{ project.name }}</small></h1>
    <section data-id="{{project.id}}">
      <p>{{#if project.author}}
          By: {{book.author}}
        {{else}}
          No author provided.
        {{/if}}
      </p>
      <button type="submit" class="btn btn-default delete-book">Remove</button>
    </section>
  </div>

{{/each}}
